<%= form_for(@event, :url => {:action => 'update'}) do |e| %>
    <%= e.label :name %>
    <%= e.text_field :name %>
    <%= e.label :description %>
    <%= e.text_area :description %>
    <%= e.submit "Edit Event" %>
<% end %>